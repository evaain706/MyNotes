

## 클로저란?

내부함수에서 외부함수에 있는 스코프에 접근할 권한을 주는것 
or
클로저는 _함수가 선언될 당시의 스코프(변수 환경)를 기억하고_,  
함수 실행이 끝난 뒤에도 그 변수에 접근할 수 있는 현상

-> `함수가 태어날 때의 환경을 기억하는 것`


``` js
function outer() {
  let count = 0;

  return function inner() {
    count++;
    console.log(count);
  };
}

const fn = outer();
fn(); // 1
fn(); // 2
fn(); // 3
```

- `outer()` 실행 → `count` 생성
- `inner` 함수가 **count를 참조**
- `outer()` 실행 종료 X
- 그런데도 `count`가 **사라지지 않음**

👉 **inner가 count를 붙잡고 있기 때문**  
👉 이 상태가 바로 **클로저**

---

## 클로저의 핵심 조건 3가지

1. **함수 안에 함수가 있고**  
2. **내부 함수가 외부 변수에 접근하며**  
3. **외부 함수 실행이 끝난 후에도 접근 가능할 때**

→  클로저 발생

---

### 그래서 왜씀?

###  상태 유지

``` js
function counter() {
  let n = 0;
  return () => ++n;
}
```

###  캡슐화 (외부 접근 차단)

```js
function createUser() {
  let password = '1234';
  return {
    check(pw) {
      return pw === password;
    }
  };
}

```

###  React Hook, 이벤트 핸들러 전부 클로저

- `useState`
- `useEffect`
- 콜백 함수