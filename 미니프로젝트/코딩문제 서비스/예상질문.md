# 기술 면접 질문 & 꼬리질문 모음집

  

## 📋 목차

1. [상태 관리 & 아키텍처](#1-상태-관리--아키텍처)

2. [React 패턴 & 최적화](#2-react-패턴--최적화)

3. [에러 처리 & 예외 상황](#3-에러-처리--예외-상황)

4. [성능 최적화](#4-성능-최적화)

5. [타입 안전성 & TypeScript](#5-타입-안전성--typescript)

6. [사용자 경험 & UX](#6-사용자-경험--ux)

7. [코드 품질 & 리팩토링](#7-코드-품질--리팩토링)

  

---

  

## 1. 상태 관리 & 아키텍처

  

### Q1-1. location.state를 사용한 편집 모드 처리

  

**면접관 질문:**

> "`CommunityForm.tsx`의 15-16번째 줄을 보면 `location.state?.post`로 편집할 게시글을 받고 있습니다. 이 방식의 문제점은 무엇이고, 더 나은 방법은 무엇일까요?"

  

**코드 위치:**

```typescript

// src/features/Community/CommunityForm.tsx:15-16

const postToEdit = location.state?.post;

const isEditMode = !!postToEdit;

```

  

**예상 답변:**

```

현재 방식의 문제점:

1. URL에 상태가 없어서 새로고침 시 데이터 손실

2. 브라우저 뒤로가기/앞으로가기 시 상태 유지 불가

3. URL만으로는 편집 모드인지 알 수 없어 디버깅 어려움

4. 북마크나 링크 공유 불가능

  

더 나은 방법:

1. URL 파라미터 사용: `/community/edit/:id`

2. React Query로 데이터 prefetch: useQuery로 게시글 조회

3. 또는 둘 다 조합: URL 파라미터로 모드 구분 + React Query로 데이터 로드

  

예시:

```typescript

const { id } = useParams();

const isEditMode = !!id;

const { data: postToEdit } = useQuery({

  queryKey: ['post', id],

  queryFn: () => fetchPost(id!),

  enabled: !!id,

});

```

```

  

**꼬리질문 1:**

> "그렇다면 왜 처음에 location.state를 사용했을까요?"

  

**예상 답변:**

```

처음에는 빠른 구현을 위해 선택했을 것입니다:

- URL 파라미터 변경 없이 기존 라우트 재사용 가능

- 별도의 API 호출 없이 이미 로드된 데이터 활용

- 하지만 프로덕션에서는 사용자 경험을 위해 URL 기반 접근이 필수입니다

```

  

**꼬리질문 2:**

> "React Query의 `enabled` 옵션을 사용한 이유는?"

  

**예상 답변:**

```

enabled 옵션은 조건부 쿼리 실행을 위해 사용합니다:

- id가 없으면 쿼리를 실행하지 않음 (불필요한 API 호출 방지)

- 타입 안전성: id가 undefined일 때 쿼리 실행 방지

- 성능 최적화: 필요한 경우에만 네트워크 요청

```

  

**핵심 키워드:**

- URL 상태 관리

- React Router의 location.state 한계

- React Query의 조건부 쿼리

- 사용자 경험 (UX)

  

---

  

### Q1-2. Zustand Store의 persist 미들웨어 사용

  

**면접관 질문:**

> "`useOptionStore`에는 persist 미들웨어를 적용했지만, `useQuizStore`에는 적용하지 않았습니다. 이렇게 구분한 이유는 무엇인가요?"

  

**코드 비교:**

```typescript

// useOptionStore.ts - persist 적용

export const useOptionStore = create<OptionState>()(

  devtools(

    persist(

      (set) => ({ ... }),

      { name: 'option-store' }

    )

  )

);

  

// useQuizStore.ts - persist 없음

export const useQuizStore = create<QuizState>((set) => ({

  quiz: null,

  userAnswer: '',

  // ...

}));

```

  

**예상 답변:**

```

useOptionStore에 persist를 적용한 이유:

- 카테고리와 난이도는 사용자가 선택한 설정값

- 페이지를 벗어났다가 돌아와도 선택한 옵션을 유지하는 것이 UX에 좋음

- 옵션 선택 페이지에서 퀴즈 페이지로 이동할 때 상태 유지 필요

  

useQuizStore에 persist를 적용하지 않은 이유:

- 퀴즈, 사용자 답안, 결과는 일시적인 상태

- 새로고침 시 새로운 퀴즈를 받아야 하므로 이전 상태 유지 불필요

- 오히려 이전 퀴즈 상태가 남아있으면 혼란스러울 수 있음

- 퀴즈는 매번 새로운 문제를 풀어야 하는 특성상 상태 유지가 불필요

  

트레이드오프:

- persist를 적용하면 로컬스토리지 I/O 비용 발생

- 하지만 옵션은 자주 변경되지 않으므로 비용 대비 효과가 큼

- 퀴즈는 매번 초기화되어야 하므로 persist 불필요

```

  

**꼬리질문 1:**

> "그렇다면 퀴즈를 풀다가 페이지를 벗어났을 때, 다시 돌아왔을 때 이어서 풀 수 있게 하려면 어떻게 해야 할까요?"

  

**예상 답변:**

```

여러 방법이 있습니다:

  

1. persist 적용 (간단하지만 제한적):

```typescript

persist(

  (set) => ({ ... }),

  { name: 'quiz-store' }

)

```

- 장점: 구현 간단

- 단점: 새로고침해도 같은 문제가 계속 보임 (새 문제 받기 어려움)

  

2. 세션 스토리지 사용:

```typescript

persist(

  (set) => ({ ... }),

  {

    name: 'quiz-store',

    storage: createJSONStorage(() => sessionStorage)

  }

)

```

- 탭이 닫히면 자동 삭제

- 새로고침해도 같은 세션 내에서는 유지

  

3. 서버에 진행 상태 저장:

- 가장 정확하지만 백엔드 작업 필요

- 사용자가 로그인한 경우에만 가능

  

4. URL에 상태 인코딩:

- 쿼리 파라미터나 해시에 상태 저장

- 하지만 데이터가 크면 URL 길이 제한 문제

```

  

**핵심 키워드:**

- 상태 지속성 (State Persistence)

- 로컬스토리지 vs 세션스토리지

- 상태 관리 전략

- 사용자 경험 고려

  

---

  

### Q1-3. Modal의 Controlled/Uncontrolled 패턴

  

**면접관 질문:**

> "`Modal.tsx`의 14-16번째 줄을 보면 controlled와 uncontrolled 모드를 모두 지원하고 있습니다. 이렇게 구현한 이유와 각각의 사용 사례를 설명해주세요."

  

**코드 위치:**

```typescript

// src/components/Modal/Modal.tsx:14-16

const isControlled = controlledIsOpen !== undefined;

const isOpen = isControlled ? controlledIsOpen : internalIsOpen;

```

  

**예상 답변:**

```

이 패턴은 "Flexible Component" 또는 "Dual Mode Component" 패턴입니다.

  

Controlled 모드 (부모가 상태 관리):

- 부모 컴포넌트가 isOpen 상태를 완전히 제어

- 여러 모달 간 상태 동기화 필요 시 유용

- 외부 상태와 연동 필요 시 사용

  

예시:

```typescript

const [isOpen, setIsOpen] = useState(false);

<Modal isOpen={isOpen} onOpenChange={setIsOpen}>

```

  

Uncontrolled 모드 (내부 상태 관리):

- 모달 자체가 내부적으로 상태 관리

- 간단한 사용 사례에 적합

- 부모 컴포넌트의 상태 관리 부담 감소

  

예시:

```typescript

<Modal>

  <Modal.Trigger>열기</Modal.Trigger>

  <Modal.Content>내용</Modal.Content>

</Modal>

```

  

왜 둘 다 지원하는가:

- 유연성: 사용하는 곳에 따라 적절한 모드 선택 가능

- 재사용성: 다양한 시나리오에서 사용 가능

- 점진적 복잡도: 간단한 경우는 uncontrolled, 복잡한 경우는 controlled

  

참고: React의 input 요소도 같은 패턴 (controlled/uncontrolled)

```

  

**꼬리질문 1:**

> "그렇다면 언제 controlled를 쓰고 언제 uncontrolled를 쓰는지 기준이 있나요?"

  

**예상 답변:**

```

기준:

  

Uncontrolled를 사용하는 경우:

- 모달 상태가 다른 컴포넌트와 독립적일 때

- 간단한 확인/취소 다이얼로그

- 폼 내부의 모달 (폼 상태와 분리)

  

Controlled를 사용하는 경우:

- 모달 상태가 외부 상태와 연동되어야 할 때

- 여러 모달 간 상태 동기화 필요

- URL 파라미터와 연동 (예: ?modal=delete)

- 전역 상태 관리 필요 시

  

현재 프로젝트에서는:

- DeleteModal: controlled (부모에서 에러 상태 관리 필요)

- IncorrectModal: controlled (선택된 퀴즈 상태 관리)

```

  

**꼬리질문 2:**

> "Modal에서 `document.body.style.overflow = 'hidden'`을 직접 조작하는 것의 문제점은?"

  

**예상 답변:**

```

문제점:

1. 사이드 이펙트: React의 선언적 패러다임과 맞지 않음

2. 여러 모달이 열릴 때: 첫 번째 모달이 닫히면 overflow가 해제되어 두 번째 모달도 스크롤 가능해짐

3. 테스트 어려움: DOM 조작을 모킹해야 함

  

개선 방안:

1. 모달 스택 관리:

```typescript

const modalStack = useRef<Set<string>>(new Set());

  

useEffect(() => {

  modalStack.current.add(modalId);

  if (modalStack.current.size === 1) {

    document.body.style.overflow = 'hidden';

  }

  return () => {

    modalStack.current.delete(modalId);

    if (modalStack.current.size === 0) {

      document.body.style.overflow = 'unset';

    }

  };

}, [modalId]);

```

  

2. Portal과 함께 CSS로 처리:

```css

.modal-overlay {

  position: fixed;

  overflow: hidden;

}

```

  

3. 라이브러리 사용: react-modal, @radix-ui/react-dialog 등

```

  

**핵심 키워드:**

- Controlled vs Uncontrolled Components

- 컴포넌트 설계 패턴

- 사이드 이펙트 관리

- React의 선언적 패러다임

  

---

  

## 2. React 패턴 & 최적화

  

### Q2-1. React.memo와 Zustand Selector의 관계

  

**면접관 질문:**

> "`OptionsCard.tsx`에 `React.memo`를 적용했지만, 내부에서 `useQuizStore`를 여러 번 호출하고 있습니다. 이 경우 `React.memo`가 실제로 최적화 효과가 있나요?"

  

**코드 위치:**

```typescript

// src/features/quizComp/OptionCard.tsx:9-13

const quiz = useQuizStore((s) => s.quiz);

const userAnswer = useQuizStore((s) => s.userAnswer);

const setUserAnswer = useQuizStore((s) => s.setUserAnswer);

const result = useQuizStore((s) => s.result);

  

// ... 132번째 줄

export default React.memo(OptionsCard);

```

  

**예상 답변:**

```

React.memo의 효과가 제한적입니다.

  

이유:

1. React.memo는 props 변경만 체크합니다

2. OptionsCard는 props가 없고, zustand store를 직접 구독합니다

3. store가 변경되면 OptionsCard는 리렌더링됩니다

4. React.memo는 store 변경을 감지하지 못합니다

  

실제 동작:

- 부모 컴포넌트가 리렌더링되어도 OptionsCard는 리렌더링되지 않음 (이 부분은 효과적)

- 하지만 store 변경 시에는 리렌더링됨 (React.memo와 무관)

  

더 효과적인 최적화:

1. Selector 통합:

```typescript

const { quiz, userAnswer, result, setUserAnswer } = useQuizStore(

  (s) => ({

    quiz: s.quiz,

    userAnswer: s.userAnswer,

    result: s.result,

    setUserAnswer: s.setUserAnswer,

  }),

  shallow

);

```

  

2. 개별 selector 최적화:

```typescript

// 각 selector가 필요한 값만 구독

const quiz = useQuizStore((s) => s.quiz);

// quiz가 변경될 때만 리렌더링

```

  

3. React.memo 제거:

- 실제로는 selector 최적화가 더 중요

- React.memo는 props 기반 컴포넌트에 효과적

```

  

**꼬리질문 1:**

> "그렇다면 React.memo를 완전히 제거해야 하나요?"

  

**예상 답변:**

```

아니요, 완전히 제거할 필요는 없습니다.

  

React.memo가 여전히 효과적인 경우:

- 부모 컴포넌트(QuizScreen)가 리렌더링될 때

- QuizScreen의 다른 상태 변경으로 인한 리렌더링 시

- OptionsCard의 props가 없어도 부모 리렌더링 방지 효과는 있음

  

하지만:

- 실제 최적화 효과는 selector 최적화가 더 큼

- React.memo는 "추가 보험" 정도의 역할

- 성능 프로파일링으로 실제 효과 측정 필요

  

결론:

- React.memo는 유지하되, selector 최적화를 먼저 진행

- 두 가지를 함께 사용하면 최대 효과

```

  

**꼬리질문 2:**

> "Zustand의 shallow 비교는 어떻게 동작하나요?"

  

**예상 답변:**

```

Zustand의 기본 동작:

- selector가 객체를 반환하면 참조 비교 (===)

- 객체 내용이 같아도 참조가 다르면 변경으로 감지

  

shallow 비교:

- 객체의 첫 번째 레벨만 비교

- 깊은 비교가 아니므로 성능상 이점

  

예시:

```typescript

// shallow 없이

const state = useQuizStore((s) => ({ quiz: s.quiz, userAnswer: s.userAnswer }));

// 매번 새 객체 생성 → 항상 변경으로 감지

  

// shallow 사용

const state = useQuizStore(

  (s) => ({ quiz: s.quiz, userAnswer: s.userAnswer }),

  shallow

);

// 객체 내용이 같으면 변경으로 감지하지 않음

```

  

주의사항:

- 중첩된 객체는 비교하지 않음

- 깊은 객체 구조에는 적합하지 않음

```

  

**핵심 키워드:**

- React.memo의 동작 원리

- Zustand selector 최적화

- 얕은 비교 (Shallow Comparison)

- 성능 측정 및 프로파일링

  

---

  

### Q2-2. useEffect의 의존성 배열 관리

  

**면접관 질문:**

> "`CommunityForm.tsx`의 50번째 줄을 보면 `useEffect`의 의존성 배열에 `reset`이 포함되어 있습니다. `reset`은 `react-hook-form`에서 제공하는 함수인데, 이게 의존성 배열에 들어가는 것이 맞나요?"

  

**코드 위치:**

```typescript

// src/features/Community/CommunityForm.tsx:29-50

useEffect(() => {

  if (isEditMode && postToEdit) {

    reset({

      title: postToEdit.title,

      content: postToEdit.content,

      category: postToEdit.category,

      nickname: postToEdit.nickname,

    });

    return;

  }

  

  if (wrongQuiz) {

    const mapped = changeIncorrectToQuestion(wrongQuiz);

    reset({

      title: mapped.title ?? '',

      content: mapped.content ?? '',

      category: mapped.category ?? 'question',

      nickname: '',

    });

  }

}, [isEditMode, postToEdit, wrongQuiz, reset]); // reset이 의존성에 포함됨

```

  

**예상 답변:**

```

이것은 실제로 문제가 될 수 있습니다.

  

문제점:

1. react-hook-form의 reset 함수는 안정적인 참조를 보장하지 않을 수 있음

2. reset이 변경되면 useEffect가 불필요하게 재실행될 수 있음

3. ESLint 경고가 발생할 수 있음

  

하지만 react-hook-form v7+에서는:

- reset 함수는 안정적인 참조를 가짐

- useForm의 반환값은 컴포넌트 생명주기 동안 안정적

- 따라서 의존성 배열에 포함해도 큰 문제는 없음

  

더 안전한 방법:

1. reset을 의존성에서 제외 (ESLint disable):

```typescript

useEffect(() => {

  // ...

}, [isEditMode, postToEdit, wrongQuiz]);

// eslint-disable-next-line react-hook-form/exhaustive-deps

```

  

2. useCallback으로 reset 래핑 (불필요함):

- reset 자체가 이미 안정적이므로 불필요

  

3. 현재 방식 유지:

- react-hook-form 문서에서도 reset을 의존성에 포함하는 것을 허용

- 실제로 문제가 발생하지 않으면 유지 가능

```

  

**꼬리질문 1:**

> "그렇다면 언제 함수를 의존성 배열에 포함해야 하나요?"

  

**예상 답변:**

```

함수를 의존성 배열에 포함해야 하는 경우:

1. 함수가 props나 state에 의존할 때

2. 함수가 매 렌더링마다 새로 생성될 때

3. 함수 내부에서 최신 값을 참조해야 할 때

  

함수를 의존성 배열에서 제외해도 되는 경우:

1. 함수가 안정적인 참조를 가질 때 (useCallback, useMemo)

2. 함수가 외부 라이브러리에서 제공되고 안정적일 때

3. 함수가 순수 함수이고 외부 의존성이 없을 때

  

현재 케이스:

- reset은 react-hook-form이 제공하는 안정적인 함수

- 하지만 ESLint 규칙을 따르려면 포함하는 것이 좋음

- 실제로 문제가 없으면 포함해도 무방

```

  

**꼬리질문 2:**

> "이 useEffect가 여러 조건을 처리하는데, 더 나은 구조는 없나요?"

  

**예상 답변:**

```

현재 구조의 문제점:

- 하나의 useEffect에서 여러 케이스 처리

- 조건이 복잡해지면 가독성 저하

  

개선 방안:

  

1. useEffect 분리:

```typescript

// 편집 모드

useEffect(() => {

  if (isEditMode && postToEdit) {

    reset({

      title: postToEdit.title,

      content: postToEdit.content,

      category: postToEdit.category,

      nickname: postToEdit.nickname,

    });

  }

}, [isEditMode, postToEdit, reset]);

  

// 오답에서 가져오기

useEffect(() => {

  if (wrongQuiz) {

    const mapped = changeIncorrectToQuestion(wrongQuiz);

    reset({

      title: mapped.title ?? '',

      content: mapped.content ?? '',

      category: mapped.category ?? 'question',

      nickname: '',

    });

  }

}, [wrongQuiz, reset]);

```

  

2. 커스텀 훅으로 분리:

```typescript

const useFormInitialization = (isEditMode, postToEdit, wrongQuiz) => {

  const { reset } = useForm();

  useEffect(() => {

    if (isEditMode && postToEdit) {

      // 편집 모드 초기화

    } else if (wrongQuiz) {

      // 오답에서 가져오기

    }

  }, [isEditMode, postToEdit, wrongQuiz]);

  return reset;

};

```

  

장점:

- 각 useEffect의 책임이 명확

- 테스트하기 쉬움

- 유지보수 용이

```

  

**핵심 키워드:**

- useEffect 의존성 배열

- 함수 참조 안정성

- React Hook Form의 API

- 코드 구조화

  

---

  

### Q2-3. useCallback의 사용 여부

  

**면접관 질문:**

> "`OptionSelect.tsx`에서 `handleCategorySelect`와 `handleLevelSelect`에 `useCallback`을 사용했는데, `useLogout.ts`의 `logout` 함수에도 `useCallback`을 사용했습니다. 어떤 기준으로 `useCallback`을 사용하나요?"

  

**코드 비교:**

```typescript

// OptionSelect.tsx

const handleCategorySelect = useCallback(

  (selectedCat: string) => {

    if (category === selectedCat) {

      setCategory('');

    } else {

      setCategory(selectedCat);

    }

  },

  [category, setCategory],

);

  

// useLogout.ts

const logout = useCallback(async () => {

  // ...

}, [clearUser, navigate, addToat]);

```

  

**예상 답변:**

```

useCallback을 사용하는 기준:

  

1. 함수를 props로 전달할 때:

- 자식 컴포넌트가 React.memo로 최적화되어 있을 때

- 함수 참조가 변경되면 자식 컴포넌트가 불필요하게 리렌더링됨

  

2. 함수를 의존성 배열에 넣을 때:

- useEffect, useMemo 등의 의존성 배열에 함수가 포함될 때

- 함수 참조가 안정적이어야 함

  

3. 커스텀 훅에서 반환하는 함수:

- 다른 컴포넌트에서 사용할 함수를 반환할 때

- useLogout처럼 다른 곳에서 사용되는 함수

  

현재 케이스 분석:

  

OptionSelect의 handleCategorySelect:

- props로 전달되지 않음

- 의존성 배열에 포함되지 않음

- useCallback이 불필요할 수 있음

- 하지만 category가 자주 변경되지 않으므로 큰 문제는 없음

  

useLogout의 logout:

- 다른 컴포넌트에서 사용됨

- 함수 참조가 안정적이어야 함

- useCallback이 적절함

  

결론:

- useCallback은 "필요할 때만" 사용

- 성능 측정 후 실제 효과가 있을 때 사용

- 조기 최적화는 피하는 것이 좋음

```

  

**꼬리질문 1:**

> "그렇다면 모든 함수에 useCallback을 적용하면 안 되나요?"

  

**예상 답변:**

```

안 됩니다. 이유:

  

1. 메모리 오버헤드:

- useCallback 자체도 메모리를 사용

- 모든 함수에 적용하면 오히려 메모리 낭비

  

2. 코드 복잡도 증가:

- 의존성 배열 관리 필요

- 코드 가독성 저하

  

3. 실제 효과 없음:

- 함수가 props로 전달되지 않으면 효과 없음

- 자식 컴포넌트가 최적화되지 않으면 효과 없음

  

올바른 접근:

1. 먼저 코드 작성

2. 성능 문제 발견

3. 프로파일링으로 병목 지점 확인

4. 필요한 곳에만 최적화 적용

  

"Measure, don't guess"

```

  

**핵심 키워드:**

- useCallback의 사용 시점

- 함수 참조 안정성

- 조기 최적화의 함정

- 성능 측정 및 프로파일링

  

---

  

## 3. 에러 처리 & 예외 상황

  

### Q3-1. ErrorBoundary와 조건부 에러 처리

  

**면접관 질문:**

> "`QuestionCard.tsx`에서 `if (error) throw error`로 에러를 던지고 ErrorBoundary에서 잡고 있습니다. 이 패턴의 문제점과 더 나은 방법은?"

  

**코드 위치:**

```typescript

// src/features/quizComp/QuestionCard.tsx:12-14

if (error) {

  throw error;

}

  

// QuizScreen.tsx:31-44

<ErrorBoundary

  fallback={(reset) => (

    <div>

      <p>퀴즈 불러오기 실패</p>

      <Button onClick={() => { reset(); fetchQuiz(); }}>다시 시도</Button>

    </div>

  )}

>

  <QuestionCard error={error} isLoading={isLoading} quiz={quiz} />

</ErrorBoundary>

```

  

**예상 답변:**

```

현재 패턴의 문제점:

  

1. ErrorBoundary의 목적과 맞지 않음:

- ErrorBoundary는 "예상치 못한" 에러를 잡기 위한 안전망

- 예상 가능한 에러(API 실패)는 조건부 렌더링으로 처리하는 것이 적절

  

2. 의도가 불명확:

- 코드를 읽는 사람이 "왜 throw하는가?"를 이해하기 어려움

- 일반적이지 않은 패턴

  

3. 에러 처리 흐름이 복잡:

- 에러 발생 → throw → ErrorBoundary → fallback 렌더링

- 직접 조건부 렌더링이 더 명확

  

더 나은 방법:

  

1. 조건부 렌더링 (추천):

```typescript

const QuestionCard = ({ error, isLoading, quiz, onRetry }) => {

  if (error) {

    return (

      <div className="error-container">

        <p>퀴즈 불러오기 실패</p>

        <Button onClick={onRetry}>다시 시도</Button>

      </div>

    );

  }

  if (isLoading) {

    return <LoadingSpinner />;

  }

  return <QuizContent quiz={quiz} />;

};

```

  

2. ErrorBoundary는 예상치 못한 에러용:

```typescript

// 컴포넌트 내부에서 예상치 못한 에러 발생 시

const QuestionCard = ({ quiz }) => {

  // 만약 quiz가 null인데 접근하면 에러 발생

  // 이런 경우 ErrorBoundary가 잡음

  return <div>{quiz.question}</div>;

};

```

  

장점:

- 코드 의도가 명확

- 에러 처리 흐름이 단순

- 테스트하기 쉬움

```

  

**꼬리질문 1:**

> "그렇다면 ErrorBoundary는 언제 사용해야 하나요?"

  

**예상 답변:**

```

ErrorBoundary 사용 시나리오:

  

1. 예상치 못한 에러:

- 컴포넌트 렌더링 중 발생하는 런타임 에러

- null/undefined 접근, 배열 인덱스 오류 등

- 개발자가 놓친 예외 상황

  

2. 전체 앱을 보호:

- 최상위에 ErrorBoundary 배치

- 예상치 못한 에러로 앱이 완전히 멈추는 것 방지

  

3. 부분적 에러 격리:

- 특정 섹션의 에러가 전체 앱에 영향 주지 않도록

- 예: 사이드바 에러가 메인 콘텐츠에 영향 주지 않음

  

사용하지 말아야 할 경우:

- 예상 가능한 에러 (API 실패, 유효성 검사 실패 등)

- 비동기 에러 (Promise rejection)

- 이벤트 핸들러 내부 에러

  

현재 프로젝트에서:

- 퀴즈 API 실패는 예상 가능한 에러 → 조건부 렌더링

- 컴포넌트 렌더링 중 예상치 못한 에러 → ErrorBoundary

```

  

**핵심 키워드:**

- ErrorBoundary의 목적

- 예상 가능한 vs 예상치 못한 에러

- 에러 처리 전략

- 사용자 경험 고려

  

---

  

### Q3-2. Axios Interceptor의 에러 처리

  

**면접관 질문:**

> "`instance.ts`의 인터셉터에서 재시도 로직을 구현했는데, `retryCounts` Map을 사용하고 있습니다. 이 방식의 문제점과 개선 방법은?"

  

**코드 위치:**

```typescript

// src/apis/instance.ts:47-77

const retryCounts = new Map<string, number>();

  

instance.interceptors.response.use(

  (res) => res,

  async (err) => {

    const config = err.config;

    const currentRetry = retryCounts.get(config.url) || 0;

    if (

      (err.message === 'Network Error' ||

       (err.response && err.response.status >= 500)) &&

      currentRetry < MAX_RETRY

    ) {

      retryCounts.set(config.url, currentRetry + 1);

      return instance(config);

    }

    retryCounts.delete(config.url);

    return Promise.reject(new Error(getErrorMessage(err)));

  },

);

```

  

**예상 답변:**

```

현재 방식의 문제점:

  

1. 메모리 누수 가능성:

- Map이 계속 쌓일 수 있음

- 성공한 요청의 카운트는 삭제되지만, 실패한 요청은 남을 수 있음

  

2. URL만으로 구분:

- 같은 URL에 다른 파라미터가 있어도 같은 키로 취급

- 예: /api/posts?page=1과 /api/posts?page=2가 같은 키

  

3. 동시 요청 처리:

- 같은 URL에 여러 요청이 동시에 발생하면 카운트가 공유됨

- 각 요청의 재시도 횟수를 독립적으로 관리하지 못함

  

4. 테스트 어려움:

- 전역 Map을 사용하므로 테스트 간 상태 공유

- 테스트 격리 어려움

  

개선 방법:

  

1. 요청별 재시도 추적:

```typescript

const retryCounts = new WeakMap<AxiosRequestConfig, number>();

  

instance.interceptors.response.use(

  (res) => {

    retryCounts.delete(res.config);

    return res;

  },

  async (err) => {

    const config = err.config;

    const currentRetry = retryCounts.get(config) || 0;

    if (shouldRetry(err) && currentRetry < MAX_RETRY) {

      retryCounts.set(config, currentRetry + 1);

      await delay(1000 * (currentRetry + 1)); // exponential backoff

      return instance(config);

    }

    retryCounts.delete(config);

    return Promise.reject(new Error(getErrorMessage(err)));

  },

);

```

  

2. 요청 ID 사용:

```typescript

let requestId = 0;

const retryCounts = new Map<number, number>();

  

const config = {

  ...err.config,

  requestId: err.config.requestId || ++requestId,

};

```

  

3. 라이브러리 사용:

- axios-retry 같은 전용 라이브러리

- 이미 검증된 로직 사용

```

  

**꼬리질문 1:**

> "WeakMap을 사용한 이유는?"

  

**예상 답변:**

```

WeakMap의 특징:

1. 키가 객체여야 함 (원시 타입 불가)

2. 가비지 컬렉션: 키 객체가 참조되지 않으면 자동 삭제

3. 이터러블하지 않음 (열거 불가)

  

장점:

- 메모리 관리 자동화

- 요청이 완료되면 자동으로 정리됨

- 메모리 누수 방지

  

단점:

- 키가 문자열이면 사용 불가 (AxiosRequestConfig 객체여야 함)

- 디버깅 어려움 (내용 확인 불가)

  

대안:

- Map 사용 + 수동 정리

- 또는 요청 완료 시 명시적으로 삭제

```

  

**핵심 키워드:**

- 메모리 관리

- 재시도 로직

- 동시성 처리

- WeakMap vs Map

  

---

  

## 4. 성능 최적화

  

### Q4-1. useInfiniteScroll의 구현 방식

  

**면접관 질문:**

> "`useInfiniteScroll.ts`에서 IntersectionObserver를 사용했는데, 왜 이 방식을 선택했나요? 스크롤 이벤트 리스너와 비교했을 때 장단점은?"

  

**코드 위치:**

```typescript

// src/hooks/useInfiniteScroll.ts

const useInfiniteScroll = ({

  loading,

  hasNextPage,

  threshold = 150,

  onLoadMore,

}: useInfiniteScrollType) => {

  const targetRef = useRef<HTMLDivElement>(null);

  

  useEffect(() => {

    if (loading || !hasNextPage) return;

  

    const observerCallback = (entries: IntersectionObserverEntry[]) => {

      if (entries[0].isIntersecting) {

        onLoadMore();

      }

    };

  

    const observer = new IntersectionObserver(observerCallback, {

      root: null,

      rootMargin: `0px 0px ${threshold}px 0px`,

      threshold: 0,

    });

  

    if (targetRef.current) {

      observer.observe(targetRef.current);

    }

  

    return () => {

      if (targetRef.current) {

        observer.unobserve(targetRef.current);

      }

    };

  }, [loading, hasNextPage, onLoadMore, threshold]);

  

  return targetRef;

};

```

  

**예상 답변:**

```

IntersectionObserver를 선택한 이유:

  

장점:

1. 성능:

- 브라우저 네이티브 API로 최적화됨

- 스크롤 이벤트보다 훨씬 효율적

- 메인 스레드 블로킹 최소화

  

2. 정확성:

- 요소가 뷰포트에 들어오는 시점을 정확히 감지

- threshold로 미리 로드 가능 (150px 전에 감지)

  

3. 간단한 구현:

- 스크롤 위치 계산 불필요

- 복잡한 수학 계산 없음

  

스크롤 이벤트 리스너와 비교:

  

스크롤 이벤트의 문제점:

1. 성능:

- 스크롤할 때마다 이벤트 발생 (매우 빈번)

- throttle/debounce 필요

- 메인 스레드 부하

  

2. 복잡한 계산:

```typescript

const handleScroll = () => {

  const scrollTop = window.scrollY;

  const windowHeight = window.innerHeight;

  const documentHeight = document.documentElement.scrollHeight;

  if (scrollTop + windowHeight >= documentHeight - threshold) {

    onLoadMore();

  }

};

```

  

3. 정확도:

- 레이아웃 변경 시 재계산 필요

- 동적 콘텐츠 높이 변화 대응 어려움

  

IntersectionObserver의 장점:

- 브라우저가 자동으로 최적화

- 레이아웃 변경 자동 감지

- 코드가 간단하고 명확

  

트레이드오프:

- IntersectionObserver는 모든 브라우저에서 지원 (IE 제외)

- 폴리필 필요 시 번들 사이즈 증가 가능

- 하지만 현재는 표준이므로 문제 없음

```

  

**꼬리질문 1:**

> "threshold를 150px로 설정한 이유는?"

  

**예상 답변:**

```

150px를 선택한 이유:

  

1. 사용자 경험:

- 사용자가 스크롤을 멈추기 전에 미리 로드

- 로딩 시간을 사용자가 느끼지 못하게 함

- 부드러운 무한 스크롤 경험

  

2. 네트워크 고려:

- 너무 크면 불필요한 데이터 로드

- 너무 작으면 로딩이 보일 수 있음

- 150px는 실험적으로 좋은 균형점

  

3. 화면 크기:

- 모바일: 작은 화면이므로 150px도 충분

- 데스크톱: 큰 화면이지만 150px로도 충분

  

조정 가능:

- 네트워크 속도에 따라 조정

- 콘텐츠 크기에 따라 조정

- 사용자 설정으로 변경 가능하게 할 수도 있음

```

  

**핵심 키워드:**

- IntersectionObserver API

- 성능 최적화

- 무한 스크롤 구현

- 사용자 경험

  

---

  

## 5. 타입 안전성 & TypeScript

  

### Q5-1. any 타입 사용과 타입 추론

  

**면접관 질문:**

> "프로젝트에서 `any` 타입을 여러 곳에서 사용했습니다. 특히 `CommentForm.tsx`의 `mutate: any`와 `useCommunity.ts`의 `onError: (error: any) => {}`가 눈에 띕니다. 이 부분을 어떻게 개선할 수 있나요?"

  

**코드 위치:**

```typescript

// CommentForm.tsx:11

mutate: any;

  

// useCommunity.ts:72, 90, 119, 138

onError: (error: any) => { ... }

```

  

**예상 답변:**

```

any 타입의 문제점:

  

1. 타입 안전성 상실:

- 컴파일 타임 에러 체크 불가

- 런타임 에러 가능성 증가

  

2. IDE 지원 부족:

- 자동완성 불가

- 리팩토링 어려움

  

3. 코드 가독성:

- 의도가 불명확

- 다른 개발자가 이해하기 어려움

  

개선 방법:

  

1. CommentForm의 mutate 타입:

```typescript

import type { UseMutationResult } from '@tanstack/react-query';

  

interface AddCommentParams {

  postId: string;

  nickname: string;

  content: string;

  password?: string;

}

  

interface CommentFormProps {

  postId: string;

  mutate: UseMutationResult<

    unknown,

    Error,

    AddCommentParams

  >;

  isLoading: boolean;

}

```

  

2. useCommunity의 error 타입:

```typescript

// 방법 1: Error 타입 사용

onError: (error: Error) => {

  const message = error instanceof Error

    ? error.message

    : '알 수 없는 오류가 발생했습니다.';

  addToast('error', message);

}

  

// 방법 2: AxiosError 타입 사용

import type { AxiosError } from 'axios';

  

onError: (error: AxiosError<{ message?: string }>) => {

  const message = error.response?.data?.message

    || error.message

    || '알 수 없는 오류가 발생했습니다.';

  addToast('error', message);

}

  

// 방법 3: 커스텀 에러 타입

interface ApiError {

  message: string;

  status?: number;

}

  

const isApiError = (error: unknown): error is ApiError => {

  return typeof error === 'object'

    && error !== null

    && 'message' in error;

};

  

onError: (error: unknown) => {

  if (isApiError(error)) {

    addToast('error', error.message);

  } else {

    addToast('error', '알 수 없는 오류가 발생했습니다.');

  }

}

```

  

장점:

- 타입 안전성 확보

- IDE 자동완성 지원

- 런타임 에러 감소

- 코드 가독성 향상

```

  

**꼬리질문 1:**

> "타입 가드(type guard)를 사용한 이유는?"

  

**예상 답변:**

```

타입 가드의 필요성:

  

1. unknown 타입 처리:

- unknown은 any보다 안전하지만 사용 전 타입 체크 필요

- 타입 가드로 런타임에 타입 확인

  

2. 타입 좁히기 (Type Narrowing):

- TypeScript가 타입을 정확히 추론하도록 도움

- 조건문 이후 타입이 좁혀짐

  

3. 에러 처리:

- 다양한 형태의 에러를 안전하게 처리

- 예상치 못한 에러 형태에도 대응

  

예시:

```typescript

// 타입 가드 없이

const handleError = (error: unknown) => {

  // error.message 접근 불가 (unknown 타입)

  console.log(error.message); // ❌ 에러

}

  

// 타입 가드 사용

const isError = (error: unknown): error is Error => {

  return error instanceof Error;

};

  

const handleError = (error: unknown) => {

  if (isError(error)) {

    console.log(error.message); // ✅ OK

  }

}

```

  

실무에서:

- 외부 API 응답 처리

- 사용자 입력 검증

- 에러 처리

- 동적 데이터 처리

```

  

**핵심 키워드:**

- 타입 안전성

- TypeScript의 any vs unknown

- 타입 가드 (Type Guard)

- 타입 좁히기 (Type Narrowing)

  

---

  

## 6. 사용자 경험 & UX

  

### Q6-1. 로딩 상태와 스켈레톤 UI

  

**면접관 질문:**

> "프로젝트에서 로딩 상태를 처리하는 방식이 다양합니다. `isPending`일 때 스켈레톤을 보여주는 경우도 있고, `isLoading`일 때 스피너를 보여주는 경우도 있습니다. 어떤 기준으로 선택했나요?"

  

**코드 비교:**

```typescript

// CommunityMain.tsx

if (isPending) return <CommunityPageSkeleton />;

  

// QuestionCard.tsx

if (isLoading) {

  return <div><Spinner /></div>;

}

  

// OptionsCard.tsx

if (!quiz) {

  return <div><Skeleton /></div>;

}

```

  

**예상 답변:**

```

선택 기준:

  

1. 스켈레톤 UI 사용:

- 레이아웃이 복잡하고 구조가 명확할 때

- 사용자가 어떤 콘텐츠가 올지 예상할 수 있을 때

- 레이아웃 시프트(CLS) 방지

- 예: CommunityPageSkeleton, UserStatisticsSkeleton

  

2. 스피너 사용:

- 간단한 로딩 상태

- 레이아웃이 단순할 때

- 전체 화면 로딩

- 예: QuestionCard의 간단한 로딩

  

3. 조건부 렌더링:

- 데이터가 없을 때와 로딩 중을 구분

- 빈 상태와 로딩 상태를 다르게 표시

- 예: OptionsCard의 quiz가 없을 때

  

React Query의 isPending vs isLoading:

- isPending: 쿼리가 아직 실행되지 않았거나 첫 로딩 중

- isLoading: isPending || isFetching (캐시가 있어도 리패치 중이면 true)

  

실제 사용:

- 첫 로딩: isPending 사용 (스켈레톤)

- 리패치: isLoading 사용 (스피너 또는 인라인 로딩)

- 또는 둘 다 사용하여 구분

  

개선 가능:

- 일관된 네이밍 (isPending vs isLoading)

- 공통 로딩 컴포넌트 사용

- 로딩 상태 관리 표준화

```

  

**꼬리질문 1:**

> "스켈레톤 UI를 직접 만든 이유는? 라이브러리를 사용하지 않은 이유는?"

  

**예상 답변:**

```

직접 만든 이유:

  

1. 디자인 일관성:

- 프로젝트의 디자인 시스템과 일치

- 브랜드 아이덴티티 유지

  

2. 번들 사이즈:

- 라이브러리 추가 시 번들 크기 증가

- 필요한 기능만 구현

  

3. 커스터마이징:

- 프로젝트에 맞게 자유롭게 수정 가능

- 특수한 요구사항 대응 가능

  

4. 학습:

- 직접 구현하면서 이해도 향상

- 나중에 수정하기 쉬움

  

하지만:

- 라이브러리 사용도 장점이 있음

- react-loading-skeleton 같은 라이브러리는 검증됨

- 개발 시간 단축

  

트레이드오프:

- 직접 구현: 시간 투자, 하지만 완전한 제어

- 라이브러리: 빠른 개발, 하지만 커스터마이징 제한

  

현재 프로젝트:

- 스켈레톤이 간단하므로 직접 구현이 적절

- 복잡해지면 라이브러리 고려 가능

```

  

**핵심 키워드:**

- 로딩 상태 관리

- 스켈레톤 UI vs 스피너

- 사용자 경험

- 레이아웃 시프트 (CLS)

  

---

  

## 7. 코드 품질 & 리팩토링

  

### Q7-1. 네이밍과 코드 일관성

  

**면접관 질문:**

> "코드를 보니 네이밍이 일관되지 않은 부분이 있습니다. 예를 들어 `IncorrectModal.tsx`의 `setIsopen` (소문자 o)와 `useLogout.ts`의 `addToat` (오타)가 있습니다. 이런 부분을 어떻게 관리하나요?"

  

**코드 위치:**

```typescript

// IncorrectModal.tsx:10

setIsopen: (open: boolean) => void; // setIsOpen이어야 함

  

// useLogout.ts:10, 19, 22

const addToat = useToastStore((state) => state.addToast); // addToast여야 함

```

  

**예상 답변:**

```

이런 문제를 방지하는 방법:

  

1. ESLint 규칙:

```json

{

  "rules": {

    "camelcase": "error",

    "@typescript-eslint/naming-convention": [

      "error",

      {

        "selector": "variable",

        "format": ["camelCase", "PascalCase"]

      }

    ]

  }

}

```

  

2. TypeScript 설정:

```json

{

  "compilerOptions": {

    "strict": true,

    "noImplicitAny": true

  }

}

```

  

3. 코드 리뷰:

- PR에서 네이밍 확인

- 일관성 체크리스트

  

4. 자동화:

- Pre-commit hook (husky)

- Lint-staged로 커밋 전 체크

  

5. 네이밍 컨벤션 문서:

- 팀 내 네이밍 규칙 명시

- 예: boolean은 is/has/should로 시작

  

현재 문제:

- 오타: addToat → addToast

- 네이밍: setIsopen → setIsOpen

- 일관성: 일부는 camelCase, 일부는 다름

  

개선:

- ESLint 규칙 추가

- Pre-commit hook 설정

- 코드 리뷰 강화

```

  

**꼬리질문 1:**

> "그렇다면 이런 오타를 찾는 방법은?"

  

**예상 답변:**

```

오타 발견 방법:

  

1. IDE 기능:

- VS Code의 오타 검사

- Spell checker 확장

  

2. 정적 분석 도구:

- ESLint

- TypeScript 컴파일러

- SonarQube

  

3. 테스트:

- 단위 테스트로 함수 호출 확인

- 통합 테스트로 실제 동작 확인

  

4. 코드 리뷰:

- 다른 개발자가 코드 확인

- 페어 프로그래밍

  

5. 자동화:

- CI/CD에서 린트 체크

- Pre-commit hook

  

실제로:

- TypeScript가 타입 에러로 잡아낼 수 있음

- 하지만 변수명 오타는 런타임까지 발견 안 될 수 있음

- 따라서 ESLint와 코드 리뷰가 중요

```

  

**핵심 키워드:**

- 코드 품질

- 네이밍 컨벤션

- 정적 분석

- 코드 리뷰

  

---

  

### Q7-2. 조건부 스타일링의 복잡도

  

**면접관 질문:**

> "`AnswerHistoryCard.tsx`의 24-42번째 줄을 보면 조건부로 className을 만드는 로직이 복잡합니다. 이 부분을 어떻게 개선할 수 있나요?"

  

**코드 위치:**

```typescript

// AnswerHistoryCard.tsx:24-42

if (data.topic?.includes('React')) {

  topicClassName = 'text-blue-500';

} else if (data.topic?.includes('TypeScript')) {

  topicClassName = 'text-sky-300';

} else if (data.topic?.includes('JavaScript')) {

  topicClassName = 'text-yellow-500';

} else if (data.topic?.includes('CSS')) {

  topicClassName = 'text-pink-500';

} else {

  topicClassName = 'text-white/80';

}

  

if (data.level === '쉬움') {

  levelClassName += 'text-green-300';

} else if (data.level === '보통') {

  levelClassName += 'text-yellow-300';

} else {

  levelClassName += 'text-red-600';

}

```

  

**예상 답변:**

```

개선 방법:

  

1. 맵 객체 사용:

```typescript

const TOPIC_COLORS: Record<string, string> = {

  React: 'text-blue-500',

  TypeScript: 'text-sky-300',

  JavaScript: 'text-yellow-500',

  CSS: 'text-pink-500',

};

  

const LEVEL_COLORS: Record<string, string> = {

  '쉬움': 'text-green-300',

  '보통': 'text-yellow-300',

  '어려움': 'text-red-600',

};

  

const getTopicColor = (topic: string) => {

  const matchedKey = Object.keys(TOPIC_COLORS).find(key =>

    topic?.includes(key)

  );

  return matchedKey ? TOPIC_COLORS[matchedKey] : 'text-white/80';

};

  

const getLevelColor = (level: string) => {

  return LEVEL_COLORS[level] || 'text-white/80';

};

  

// 사용

const topicClassName = getTopicColor(data.topic);

const levelClassName = getLevelColor(data.level);

```

  

2. 유틸 함수로 분리:

```typescript

// utils/getTopicColor.ts

export const getTopicColor = (topic: string): string => {

  const colorMap: Record<string, string> = {

    React: 'text-blue-500',

    TypeScript: 'text-sky-300',

    JavaScript: 'text-yellow-500',

    CSS: 'text-pink-500',

  };

  const matchedKey = Object.keys(colorMap).find(key =>

    topic?.includes(key)

  );

  return matchedKey ? colorMap[matchedKey] : 'text-white/80';

};

```

  

3. clsx/cn 유틸 사용:

```typescript

import { cn } from '@/utils/cn';

  

const topicClassName = cn(

  'font-bold',

  getTopicColor(data.topic)

);

```

  

장점:

- 가독성 향상

- 유지보수 용이 (색상 변경 시 한 곳만 수정)

- 테스트하기 쉬움

- 재사용 가능

```

  

**꼬리질문 1:**

> "레벨 클래스명에서 `+=`를 사용한 이유는? 그리고 문제점은?"

  

**예상 답변:**

```

문제점:

  

1. 초기값 없음:

- levelClassName이 빈 문자열로 시작

- += 연산자 사용 시 예상치 못한 동작 가능

  

2. 공백 누락:

- 'text-green-300' + 'text-yellow-300' = 'text-green-300text-yellow-300'

- 클래스명이 붙어서 잘못된 스타일 적용

  

3. 조건부 추가 어려움:

- 여러 클래스를 추가하려면 복잡해짐

  

올바른 방법:

  

1. 직접 할당:

```typescript

const levelClassName = data.level === '쉬움'

  ? 'text-green-300'

  : data.level === '보통'

  ? 'text-yellow-300'

  : 'text-red-600';

```

  

2. 맵 사용:

```typescript

const levelClassName = LEVEL_COLORS[data.level] || 'text-white/80';

```

  

3. cn 유틸 사용:

```typescript

const levelClassName = cn(

  'font-bold',

  LEVEL_COLORS[data.level] || 'text-white/80'

);

```

  

현재 코드의 문제:

- levelClassName이 초기화되지 않음

- += 연산자로 인한 예상치 못한 동작 가능

- 공백 처리 없음

```

  

**핵심 키워드:**

- 코드 리팩토링

- 조건부 로직 개선

- 유틸 함수 분리

- 가독성 향상

  

---

  

## 📝 종합 정리

  

### 면접 대비 체크리스트

  

**기술적 깊이:**

- [ ] 상태 관리 도구 선택 기준 설명 가능

- [ ] React 패턴 (Controlled/Uncontrolled) 이해

- [ ] 성능 최적화 전략 설명 가능

- [ ] 에러 처리 전략 설명 가능

- [ ] TypeScript 타입 시스템 이해

  

**트레이드오프 이해:**

- [ ] 각 선택의 장단점 설명 가능

- [ ] 상황에 따른 적절한 선택 기준 제시

- [ ] 실무 경험 기반 설명

  

**코드 품질:**

- [ ] 개선 가능한 부분 식별

- [ ] 리팩토링 방향 제시

- [ ] 코드 리뷰 관점 이해

  

**커뮤니케이션:**

- [ ] 기술을 비전문가에게 설명 가능

- [ ] 문제 상황 설명 및 해결 과정 설명

- [ ] 팀 협업 경험 설명

  

---

  

## 🎯 핵심 메시지

  

1. **"왜 이렇게 했는가?"** - 모든 선택에는 이유가 있어야 함

2. **"더 나은 방법은?"** - 항상 개선 가능성을 고려

3. **"트레이드오프는?"** - 완벽한 해결책은 없음, 상황에 맞는 선택

4. **"실무 경험은?"** - 이론뿐만 아니라 실제 경험 중요